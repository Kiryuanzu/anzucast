<div class="max-w-4xl mx-auto py-8 px-4">
  <h1 class="text-2xl font-bold mb-6">エピソード一覧</h1>

  <% @episodes.each do |episode| %>
    <div class="mb-6 border-b pb-4">
      <h2 class="text-lg font-semibold">
        <%= link_to episode.title, episode_path(episode), class: "text-blue-600 hover:underline" %>
      </h2>
      <p class="text-sm text-gray-500">公開日: <%= episode.published_at.strftime("%Y-%m-%d") %></p>
      <p class="mt-1 text-gray-800"><%= truncate(episode.description, length: 100) %></p>
    </div>
  <% end %>

  <%= paginate @episodes %>

  <section class="mt-12 rounded-md border border-gray-300 bg-white p-6 text-center shadow-sm">
    <p class="text-base text-gray-900">
      番組への感想や質問などは📮<%= link_to "こちら",
                                      "https://forms.gle/F11Rmw5mVd8a7sYHA",
                                      target: "_blank",
                                      rel: "noopener",
                                      class: "text-blue-600 hover:underline" %>から（Google Formが開きます）。お気軽にメッセージをお送りください。
    </p>
  </section>
</div>
